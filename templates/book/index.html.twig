{% extends 'base.html.twig' %}

{% block title %}Book index
{% endblock %}

{% block body %}
    <h1 style="text-align: center;">Book index</h1>
<div class="book-card">
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Autor</th>
                <th>Description</th>
                <th>Stock</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for book in books %}
            <tr>
                <td>{{ book.id }}</td>
                <td>{{ book.title }}</td>
                <td>{{ book.autor }}</td>
                <td>{{ book.description }}</td>
                <td>{{ book.stock }}</td>
                <td>
                    <a href="{{ path('app_book_show', {'id': book.id}) }}">show</a>
                    {% if is_granted('ROLE_USER') %}
                        <a href="{{ path('app_user_dashboard_book_reserve', {'id': book.id}) }}">Reserver</a>
                    {% endif %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('app_book_edit', {'id': book.id}) }}">edit</a>
                    {% endif %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('app_book_new') }}">Create new</a>
{% endif %}
{% endblock %}
